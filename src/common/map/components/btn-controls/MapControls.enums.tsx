export enum ButtonsControl {
  BTN_HOME = 'home',
  BTN_ZOOM_IN = 'zoom-in',
  BTN_ZOOM_OUT = 'zoom-out',
}

export enum Direction {
  IN = 'in',
  OUT = 'out',
}

export enum MapButtonsTooltip {
  HOME_TOOLTIP = 'common.mapData.buttons.home',
  ZOOM_IN_TOOLTIP = 'common.mapData.buttons.zoomIn',
  ZOOM_OUT_TOOLTIP = 'common.mapData.buttons.zoomOut',
}
